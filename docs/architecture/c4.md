# Initial architecture: Irudd Factory agent issue dispatcher

## Current understanding

Irudd Factory is a TypeScript service hosted on one DigitalOcean Ubuntu VM. It watches configured GitHub repositories, selects eligible issues, creates isolated per-issue workspaces, and runs Claude Code or Codex CLI sessions. A tailnet-served console lets one human operator monitor sessions and control work such as pausing providers, stopping runs, resuming providers, or guiding assignments. The exact TypeScript stack and authentication details are unknown. Based on [GitHub issue #1](https://github.com/alundgren/irudd-factory/issues/1).

Each installation permanently serves one developer working on repositories
they own. The dedicated VM is the security boundary. Per-issue workspaces
separate Git state and concurrent changes, but they do not provide filesystem
read isolation from the dispatcher, provider credentials, or other workspaces.
Issues and repository contents are trusted input.

## Release requirement

Before the first real release, add `SECURITY-LIMITATIONS.md` at the repository
root. It must describe the single-user, trusted-input deployment model, the
dedicated-VM requirement, unrestricted agent reads on that VM, credentials and
network access available to agents, possible cross-workspace interference, and
the operator's responsibility for repository access and recovery. Release work
must not close this requirement by relying only on setup documentation or a
README warning.

## System context

```mermaid
C4Context
title System context

Person(operator, "Operator", "Reviews, guides, and controls automated implementation work")
System(factory, "Irudd Factory", "Selects GitHub issues and coordinates isolated coding-agent runs")

System_Ext(github, "GitHub", "Hosts repositories, issues, pull requests, and comments")
System_Ext(agent_providers, "Agent provider CLIs", "Claude Code and Codex CLI execute implementation sessions")
System_Ext(tailnet, "Tailnet", "Provides private network access to the operator console")
System_Ext(digital_ocean, "DigitalOcean Ubuntu VM", "Hosts the Irudd Factory services")

Rel(operator, factory, "Monitors and controls work through the console")
Rel(factory, github, "Finds eligible issues and records run and pull-request attribution")
Rel(factory, agent_providers, "Starts and supervises structured coding-agent sessions")
Rel(operator, tailnet, "Connects through the private network")
Rel(tailnet, factory, "Provides private access to the console")
Rel(digital_ocean, factory, "Hosts the deployed services")
```

## Containers

```mermaid
C4Container
title Containers

Person(operator, "Operator", "Reviews, guides, and controls automated implementation work")
System_Ext(github, "GitHub", "Hosts repositories, issues, pull requests, and comments")
System_Ext(agent_providers, "Agent provider CLIs", "Claude Code and Codex CLI")
System_Ext(tailnet, "Tailnet", "Provides private network access")
System_Ext(digital_ocean, "DigitalOcean Ubuntu VM", "Runs the deployed services")

Container_Boundary(factory, "Irudd Factory") {
    Container(console, "Operator Console", "TypeScript; exact web stack unknown", "Displays assignments, sessions, provider state, logs, and available operator controls.")
    Container(dispatcher, "Dispatcher daemon", "TypeScript; exact runtime stack unknown", "Polls GitHub, selects eligible issues, reserves assignments, manages workspaces, and runs coding-agent sessions.")
    Container(state_store, "Assignment state store", "SQLite", "Persists assignment history, provider state, session identifiers, and restart-recovery state.")
    Container(run_logs, "Run log store", "JSONL files", "Stores diagnostic events for issues and sessions so completed or failed work can be investigated.")
}

Rel(operator, console, "Uses to monitor and control runs")
Rel(tailnet, console, "Provides private access")
Rel(console, dispatcher, "Reads status and sends operator commands")
Rel(dispatcher, state_store, "Reserves assignments and persists state transitions")
Rel(dispatcher, run_logs, "Writes structured diagnostic events")
Rel(dispatcher, github, "Reads candidates and writes attribution comments and pull-request links")
Rel(dispatcher, agent_providers, "Launches and supervises provider sessions")
Rel(dispatcher, digital_ocean, "Runs on the VM")
```

## Components

```mermaid
C4Component
title Dispatcher daemon components

Container_Boundary(dispatcher, "Dispatcher daemon") {
    Component(candidate_selection, "Candidate selection", "TypeScript", "Finds open, correctly labelled, unclaimed, unblocked issues in the configured repository pool using stable ordering.")
    Component(assignment_coordinator, "Assignment coordinator", "TypeScript", "Reserves issues, enforces one active session per provider, and records assignment state transitions.")
    Component(provider_runners, "Provider runners", "TypeScript", "Launches Claude Code or Codex CLI with isolated credentials and consumes structured session events.")
    Component(workspace_manager, "Workspace manager", "TypeScript", "Creates and validates app-owned clones, worktrees, and branches for each assignment.")
    Component(reconciler, "Reconciliation and stall handling", "TypeScript", "Stops work whose issue is no longer eligible and terminates sessions that stop emitting events.")
    Component(pr_attribution, "Pull-request attribution", "TypeScript", "Finds implementation pull requests and adds an idempotent attribution comment.")
}

Rel(candidate_selection, assignment_coordinator, "Offers eligible issues for reservation")
Rel(assignment_coordinator, workspace_manager, "Creates an isolated workspace for a reserved issue")
Rel(assignment_coordinator, provider_runners, "Starts one provider session for an assignment")
Rel(provider_runners, reconciler, "Reports provider events and liveness")
Rel(reconciler, assignment_coordinator, "Updates or stops stale and invalid assignments")
Rel(provider_runners, pr_attribution, "Provides completed-run identifiers and results")
Rel(pr_attribution, assignment_coordinator, "Records final run state")
```

## Sequence

```mermaid
sequenceDiagram
    participant D as Dispatcher daemon
    participant G as GitHub
    participant S as SQLite state store
    participant W as Workspace manager
    participant P as Provider runner
    participant O as Operator
    participant C as Console

    D->>G: Refresh eligible issues and currently running issues
    D->>S: Check assignment history and provider availability
    D->>S: Reserve the selected issue
    D->>W: Create and validate isolated workspace
    W-->>D: Workspace ready or failure
    D->>P: Start structured Claude or Codex session
    P-->>D: Session ID and streaming events
    D->>S: Persist state transitions and session identifiers
    D->>C: Publish current status and events
    C-->>O: Show progress, failures, and controls

    alt Issue becomes ineligible or session stalls
        D->>G: Recheck issue state
        D->>P: Stop the session
        D->>S: Record the named failure or cancellation
        D->>C: Publish stopped state
    else Provider startup or authentication failure
        D->>S: Pause the provider
        D->>C: Publish provider-paused state
        O->>C: Resume provider after correction
        C->>D: Send resume command
        D->>S: Mark provider available
    else Session completes and creates a pull request
        P-->>D: Report completion and pull-request details
        D->>G: Add idempotent attribution comment
        D->>S: Record completed assignment
        D->>C: Publish completed state
    end
```

## Material unresolved questions

- What authentication mechanism should protect the console in addition to tailnet access?
- Should operator guidance be persisted as assignment instructions, sent to a live session, or limited to lifecycle controls?
- What exact durable assignment states and restart-recovery rules are required?
- What is the exact provider protocol and launch contract for Claude Code and Codex CLI?
- What console data must remain available after JSONL logs or workspaces are archived?
- What TypeScript runtime, web framework, and process supervision approach will be selected?
- Will the VM run the console and dispatcher as separate daemons or as processes managed by one service unit?
