# Architecture: implemented dispatcher slice

Irudd Factory is a TypeScript service for one dedicated machine. It polls each
configured GitHub repository, records FIFO eligibility tenure, and fills a
bounded Codex pool. Factory revalidates each issue, reserves a slot, creates a
retained linked worktree, and runs one Codex turn. Manual starts use the same
admission transaction. A loopback-only listener serves the console in local
mode. Tailscale mode serves the same console through Tailscale Serve and keeps
CLI RPC on a separate loopback listener. The service exposes queue pages and
durable dispatch controls through Effect RPC.

One service process holds an exclusive lease for each database. Startup marks
unfinished attempts interrupted, terminates provider process groups whose
identity it can confirm, and keeps capacity occupied when ownership is
uncertain.

Codex CLI is the only provider. Application ports keep its protocol out of the
command and state modules.

Three rules apply throughout the implementation:

- Only issues whose author has write permission to the repository are eligible.
- `gh` and Codex use the operator's ambient credentials. Factory creates,
  injects, and stores no tokens.
- Codex writable roots name the worktree, the linked-worktree Git directory,
  and the shared Git directory. Reads are unrestricted on the machine.

The practical consequences are recorded in
[SECURITY-LIMITATIONS.md](../../SECURITY-LIMITATIONS.md). Technology choices are
in [stack.md](../stack.md).

## System context

```mermaid
C4Context
title System context

Person(operator, "Operator", "Starts selected issues and monitors attempts")
System(factory, "Irudd Factory", "Runs selected issues through a bounded Codex process pool")

System_Ext(github, "GitHub", "Hosts repositories, issues, policy, and pull requests")
System_Ext(codex, "Codex App Server", "Runs the unattended coding turn")
System_Ext(tailscale, "Tailscale Serve", "Forwards authenticated tailnet HTTPS requests")
System_Ext(vm, "Dedicated machine", "Holds Factory and ambient credentials")

Rel(operator, factory, "Uses the loopback CLI or local console")
Rel(operator, tailscale, "Uses the remote console over tailnet HTTPS")
Rel(tailscale, factory, "Forwards requests with operator identity")
Rel(factory, github, "Discovers and claims an issue, then verifies its pull request")
Rel(factory, codex, "Starts and supervises bounded concurrent turns")
Rel(vm, factory, "Hosts")
```

## Containers

```mermaid
C4Container
title Containers

Person(operator, "Operator", "Submits and monitors work")
System_Ext(github, "GitHub", "Repositories, issues, policy, and pull requests")
System_Ext(codex, "Codex App Server", "Structured coding provider")
System_Ext(tailscale, "Tailscale Serve", "Authenticated HTTPS proxy managed by the operator")

Container_Boundary(factory, "Irudd Factory") {
    Container(console, "Operator console", "React", "Submits commands and polls durable attempt state.")
    Container(cli, "CLI", "TypeScript", "Targets a configured repository issue and prints its admission receipt.")
    Container(service, "Factory service", "TypeScript and Effect", "Enforces local or Tailscale access, polls repositories, dispatches queued work, handles RPC, and serves the console.")
    Container(state, "State store", "SQLite", "Leases the database and stores queue tenure, controls, receipts, attempts, events, process identity, paths, diagnostics, and pull request evidence.")
}

Rel(operator, console, "Uses locally or through Tailscale Serve")
Rel(operator, cli, "Uses locally")
Rel(console, service, "Effect RPC on the local or Tailscale main listener")
Rel(cli, service, "Effect RPC on a loopback listener")
Rel(tailscale, service, "Forwards console and RPC requests to the main listener")
Rel(service, state, "Commits admission and records progress")
Rel(service, github, "Reads policy and candidates, claims, and verifies")
Rel(service, codex, "Runs one turn with ambient credentials")
```

## Package dependencies

```mermaid
flowchart LR
    contracts[packages/contracts]
    application[packages/application]
    sqlite[packages/state-sqlite]
    github[packages/github]
    workspaces[packages/workspaces]
    codex[packages/codex]
    service[apps/service]
    cli[apps/cli]
    console[apps/console]

    application --> contracts
    sqlite --> application
    github --> application
    workspaces --> application
    codex --> application
    service --> application
    service --> sqlite
    service --> github
    service --> workspaces
    service --> codex
    service --> contracts
    service --> cli
    cli --> contracts
    console --> contracts
```

`packages/application` owns command admission, state transitions, policy
parsing, prompt construction, and ports. Adapters depend on those ports.
`apps/service` owns the deterministic fixture catalog and composes production,
integration, or fixture dependencies. Each fixture definition contains its
metadata, state, fake behavior, and executable expectations. The integration
runner uses the public CLI package for the same RPC calls operators use. Neither
client imports an adapter or the application package.

`scripts/fixture.ts` is the stable development entry point. The `vp run fixture`
task is intended for interactive use. Scripts and agents invoke it through
`vp node scripts/fixture.ts` so JSON stdout contains no task-runner diagnostics.

## Dispatch lifecycle

```mermaid
sequenceDiagram
    actor Operator
    participant Client as CLI or console
    participant Service
    participant DB as SQLite
    participant GH as GitHub adapter
    participant WS as Workspace adapter
    participant CX as Codex adapter

    loop Poll interval
        Service->>GH: Discover eligible issues and pinned WORKFLOW.md
        Service->>DB: Reconcile queue tenure and eligibility loss
    end
    Service->>DB: Read FIFO queue and persistent controls
    Service->>GH: Revalidate issue and workflow revision
    alt stale issue
        Service->>DB: End tenure with rejection reason
    else startable issue
        Service->>DB: BEGIN IMMEDIATE and reserve slot and issue
        DB-->>Service: Durable admission
        Service->>GH: Add claimed exactly once
        Service->>WS: Create retained linked worktree
        Service->>CX: Run one App Server turn
        CX-->>Service: Normalized turn evidence and PR URL
        Service->>GH: Verify repository, factory branch, and closing issue
        Service->>DB: Record completed or failed
    end
```

Automatic resume or retry, cancellation, stall detection, attribution comments,
and cleanup are not in the current component set. Factory validates Tailscale
identity headers but does not install, configure, start, or stop Tailscale.
